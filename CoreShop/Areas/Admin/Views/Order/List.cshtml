@model List<CoreShop.MODEL.Entities.Order>

@{
    ViewData["Title"] = "Sipariş Yönetimi";
    Layout = "~/Areas/Shared/_AdminLayout.cshtml";
}

<div class="container-fluid">
    <div class="card border-0 shadow-sm rounded-4 p-4 mb-4">

        <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">

            <div>
                <h2 class="fw-bold mb-1">Sipariş Yönetimi</h2>
                <small class="text-muted">
                    Toplam Sipariş: @Model.Count
                </small>
            </div>

            <form method="get" class="d-flex gap-2">
                <input type="number"
                       name="searchId"
                       class="form-control"
                       placeholder="Sipariş No Ara" />

                <button class="btn btn-dark">
                    Ara
                </button>
            </form>

        </div>

    </div>

    <div class="row g-4">

        @foreach (var item in Model)
        {
            <div class="col-lg-6">

                <div class="card border-0 shadow-sm rounded-4 p-4 h-100 order-card">

                    <div class="d-flex justify-content-between mb-3">

                        <div>
                            <h5 class="fw-bold mb-1">
                                Sipariş #@item.ID
                            </h5>

                            <small class="text-muted">
                                @item.OrderDate.ToString("dd.MM.yyyy")
                            </small>
                        </div>

                        <div class="fw-bold fs-5">
                            ₺@item.TotalPrice.ToString("N2")
                        </div>

                    </div>

                    <div class="mb-3">
                        <strong>@item.FullName</strong><br />
                        <small class="text-muted">
                            @item.City / @item.District
                        </small>
                    </div>

                    <div class="mb-3">

                        @if (item.Status == "Paid")
                        {
                            <span class="badge bg-success px-3 py-2">
                                💳 Ödendi
                            </span>
                        }
                        else if (item.Status == "Hazırlanıyor")
                        {
                            <span class="badge bg-warning text-dark px-3 py-2">
                                📦 Hazırlanıyor
                            </span>
                        }
                        else if (item.Status == "Kargoda")
                        {
                            <span class="badge bg-primary px-3 py-2">
                                🚚 Kargoda
                            </span>
                        }
                        else if (item.Status == "Teslim Edildi")
                        {
                            <span class="badge bg-success px-3 py-2">
                                ✅ Teslim Edildi
                            </span>
                        }
                        else if (item.Status == "Cancelled")
                        {
                            <span class="badge bg-danger px-3 py-2">
                                ❌ İptal Edildi
                            </span>
                        }

                    </div>

                    @if (item.Status == "Teslim Edildi" || item.Status == "Cancelled")
                    {
                        <div class="d-flex gap-2">

                            <select class="form-select" disabled>
                                <option>@item.Status</option>
                            </select>

                            <button class="btn btn-secondary" disabled>
                                Kilitli
                            </button>

                        </div>
                    }
                    else
                    {
                        <form method="post"
                              asp-area="Admin"
                              asp-controller="Order"
                              asp-action="UpdateStatus">

                            <input type="hidden"
                                   name="id"
                                   value="@item.ID" />

                            <div class="d-flex gap-2">

                                <select name="status"
                                        class="form-select">

                                    @if (item.Status == "Paid")
                                    {
                                        <option value="Paid" selected disabled>
                                            Ödendi
                                        </option>

                                        <option value="Hazırlanıyor">
                                            Hazırlanıyor
                                        </option>

                                        <option value="Kargoda" disabled>
                                            Kargoda
                                        </option>

                                        <option value="Teslim Edildi" disabled>
                                            Teslim Edildi
                                        </option>

                                        <option value="Cancelled">
                                            İptal Edildi
                                        </option>
                                    }

                                    else if (item.Status == "Hazırlanıyor")
                                    {
                                        <option value="Paid" disabled>
                                            Ödendi
                                        </option>

                                        <option value="Hazırlanıyor" selected disabled>
                                            Hazırlanıyor
                                        </option>

                                        <option value="Kargoda">
                                            Kargoda
                                        </option>

                                        <option value="Teslim Edildi" disabled>
                                            Teslim Edildi
                                        </option>

                                        <option value="Cancelled">
                                            İptal Edildi
                                        </option>
                                    }

                                    else if (item.Status == "Kargoda")
                                    {
                                        <option value="Paid" disabled>
                                            Ödendi
                                        </option>

                                        <option value="Hazırlanıyor" disabled>
                                            Hazırlanıyor
                                        </option>

                                        <option value="Kargoda" selected disabled>
                                            Kargoda
                                        </option>

                                        <option value="Teslim Edildi">
                                            Teslim Edildi
                                        </option>

                                        <option value="Cancelled" disabled>
                                            İptal Edildi
                                        </option>
                                    }

                                </select>

                                <button class="btn btn-dark">
                                    Kaydet
                                </button>

                            </div>

                        </form>
                    }

                    <div class="mt-3 d-flex gap-2">
                        <a asp-area="Admin"
                           asp-controller="Order"
                           asp-action="Detail"
                           asp-route-id="@item.ID"
                           class="btn btn-outline-dark">
                            Detay
                        </a>
                    </div>

                </div>

            </div>
        }

    </div>

</div>

<style>
    .order-card {
        transition: .2s;
    }

        .order-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 16px 35px rgba(0,0,0,.08) !important;
        }
</style>